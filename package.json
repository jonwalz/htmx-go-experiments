{
  "name": "project",
  "version": "0.0.0",
  "description": "Frontend part of the Gowebly project.",
  "license": "MIT",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "fmt": "prettier --write .",
    "build": "PARCEL_WORKER_BACKEND=process parcel build --log-level=verbose ./assets/scripts.js ./assets/styles.scss --dist-dir ./static",
    "dev": "PARCEL_WORKER_BACKEND=process parcel build --log-level=verbose ./assets/scripts.js ./assets/styles.scss --dist-dir ./static --no-optimize",
    "watch": "parcel watch ./assets/scripts.js ./assets/styles.scss --dist-dir ./static"
  },
  "dependencies": {
    "@tailwindcss/forms": "latest",
    "@tailwindcss/typography": "latest",
    "tailwindcss": "latest",
    "daisyui": "latest",
    "htmx.org": "latest"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "latest",
    "autoprefixer": "latest",
    "parcel": "latest",
    "postcss": "latest",
    "prettier": "latest"
  }
}
